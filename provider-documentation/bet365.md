# gush ai Live Scores

_Auto-generated from `services/bet365/manifest.php` by `tools/gen-provider-docs.php`._
_Do not hand-edit — re-run the generator after changing the manifest instead._

| Field | Value |
|---|---|
| Key | `bet365` |
| Folder | `services/bet365/` |
| Chat command | `bet365` |
| Type | `sports` |
| Enabled | yes |
| Requires login | no |
| Billing | free |
| Visual template | yes — scope `gsc-tpl-bet365` |

Live in-play soccer scores and fixtures via RapidAPI (bet365-api-inplay).
This is a read-only scores/odds-display feed — it does not place bets, hold
funds, or touch a wallet.

> **Manifest drift fixed in this pass:** the manifest's `command` field is
> the literal string `bet365` (no `@`) and `name` is `gush ai Live Scores` —
> both had changed since the previous generated page, which still showed
> `@bet365` / "Bet365 Live Scores" and marked the visual template as
> disabled even though `template.enabled` is `true`. This page now matches
> `manifest.php` as shipped.

## Actions

- `live`
- `search`

## Calling it

```
POST /?action=service_run
Content-Type: application/json

{ "service": "bet365", "input": { "action": "live" } }
```

## Configuration

This provider ships `config.php.example` — copy it to `config.php` inside
`services/bet365/` and fill in your RapidAPI key. **This document never
shows config.php contents** — it is generated from `manifest.php` only, which
never holds secrets by convention.

## Visual template

This provider renders with its own card/modal design instead of the
default scoreboard card.

- CSS: `services/bet365/template/style.css`
- JS: `services/bet365/template/render.js`
- Scope class: `gsc-tpl-bet365`

See `HOW-TO-BUILD-A-PROVIDER.md` for the template contract.
